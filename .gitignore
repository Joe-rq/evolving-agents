# === SSOT — internal state, never published ===
state.md

# === Secrets — domain credentials NEVER enter this repo ===
.env
.env.*
*secret*
*credential*
*token*
*api_key*
*apikey*
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Local runs / data (keep repo clean of run artifacts)
runs/
output/
*.sqlite
*.db
*.jsonl

# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp
